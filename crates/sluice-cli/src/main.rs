//! `sluice` — command-line entry point for the encrypted, deduplicating backup
//! and disaster-recovery tool.
//!
//! Pre-alpha skeleton (milestone M0). The command surface is specified in
//! `DESIGN.md` §7 and implemented from milestone M1 onward.

fn main() {
    eprintln!(
        "sluice {} (pre-alpha): no commands are implemented yet — see DESIGN.md",
        env!("CARGO_PKG_VERSION")
    );
}
